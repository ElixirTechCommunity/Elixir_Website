/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['ui.aceternity.com','res.cloudinary.com', 'images.unsplash.com', 'via.placeholder.com', 'i.ibb.co'],
  },
};

export default nextConfig;
